# 基础依赖
requests>=2.31.0

# 单元测试相关（开发环境）
# unittest 是 Python 内置模块，无需安装
# mock 在 Python 3.3+ 中已集成到 unittest.mock

# 性能优化依赖（可选）
# 如果需要更快的 JSON 处理，可以取消注释下面的依赖
# orjson>=3.9.0

# 日志增强依赖（可选）
# 如果需要结构化日志，可以取消注释下面的依赖
# structlog>=23.1.0